$kind: http-request
url: "{{base_url}}/api/v1/organization/catalogs/{{catalog_id}}"
method: PUT
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "Full Blood Count (Updated)",
      "basePrice": 5500.00,
      "version": 1
    }
order: 4000
