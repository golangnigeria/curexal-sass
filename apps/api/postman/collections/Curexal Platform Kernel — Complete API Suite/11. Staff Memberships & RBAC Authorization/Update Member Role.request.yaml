$kind: http-request
url: "{{base_url}}/api/v1/organization/members/{{member_id}}/role"
method: PUT
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "role": "clinician",
      "roleTitle": "Senior Consultant Pathologist"
    }
order: 7000
