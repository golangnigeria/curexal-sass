$kind: http-request
url: "{{base_url}}/api/v1/platform/pricing"
method: PUT
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "capability": "clinical.basic",
      "monthlyPrice": 25000.00,
      "currency": "NGN"
    }
order: 12000
