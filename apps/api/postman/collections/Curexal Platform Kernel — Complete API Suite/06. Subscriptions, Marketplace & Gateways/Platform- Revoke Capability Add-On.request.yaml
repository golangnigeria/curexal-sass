$kind: http-request
name: "Platform: Revoke Capability Add-On"
url: "{{base_url}}/api/v1/platform/organizations/{{org_id}}/capabilities/{{capability}}"
method: DELETE
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
order: 9000
