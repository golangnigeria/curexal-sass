$kind: http-request
url: "{{base_url}}/api/v1/platform/payment-gateways/{{provider}}"
method: PUT
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "secretKey": "sk_live_paystack_mock_key",
      "publicKey": "pk_live_paystack_mock_key",
      "isActive": true
    }
order: 15000
