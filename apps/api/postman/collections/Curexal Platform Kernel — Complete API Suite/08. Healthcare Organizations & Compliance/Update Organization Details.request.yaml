$kind: http-request
url: "{{base_url}}/api/v1/organizations/{{org_id}}"
method: PUT
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "Everight Diagnostics Healthcare Group",
      "phone": "+2348099887766"
    }
order: 4000
