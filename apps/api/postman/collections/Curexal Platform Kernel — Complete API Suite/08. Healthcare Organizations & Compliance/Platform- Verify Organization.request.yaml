$kind: http-request
name: "Platform: Verify Organization"
url: "{{base_url}}/api/v1/platform/organizations/{{org_id}}/verify"
method: POST
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
order: 15000
