$kind: http-request
url: "{{base_url}}/api/v1/auth/accept-invite"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "token": "{{invitation_token}}",
      "name": "Dr. Florence Nwosu",
      "password": "StaffSecurePassword123!"
    }
order: 10000
