$kind: http-request
url: "{{base_url}}/api/v1/auth/impersonate"
method: POST
headers:
  Authorization: Bearer {{auth_token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "organizationId": "{{org_id}}",
      "reason": "Support investigation"
    }
order: 9000
