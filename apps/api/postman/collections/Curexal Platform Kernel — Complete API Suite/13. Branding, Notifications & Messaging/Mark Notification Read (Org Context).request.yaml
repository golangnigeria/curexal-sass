$kind: http-request
url: "{{base_url}}/api/v1/organization/notifications/{{notification_id}}/read"
method: POST
headers:
  Authorization: Bearer {{auth_token}}
  X-CSRF-Token: "{{csrf_token}}"
order: 8000
